#in this project we are building the project called the codecoach --> the full stack porject

the key component ==> 
 
techstack ==>

for the building AI Agent ==> langchain + langgraph

for the database using the ==> MYSQl

for the backend  API devlopment ==> FASTAPI

for the frontend ==> React

for the orm ==> using the Sqlalchemy + in async mode
